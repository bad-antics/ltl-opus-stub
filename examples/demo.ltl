// examples/demo.ltl — runnable demo for ltl_opus_stub

import std::io
use ltl_opus_stub as lib

fn main() {
    io::println("ltl_opus_stub demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}