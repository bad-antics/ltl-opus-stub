// ltl-opus-stub/main.ltl
//
// Opus packet parser with TOC byte decoder.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-opus-stub v0.1.0")
    io::println("Opus packet parser with TOC byte decoder.")
    0
}
